- if errors.any?
  .form-group
    ul
      - errors.each do |attr, messages|
        - messages.each do |msg|
          li
            = "#{attr}: #{msg}"
p
= simple_form_for spade_form do |f|
  = f.input :color
  = f.input :width
  = f.input :length
  = f.submit